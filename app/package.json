{
  "name": "securebank-banking-sandbox",
  "version": "3.1.1",
  "private": true,
  "description": "Functional synthetic-data Nigerian digital banking portfolio sandbox with PostgreSQL persistence",
  "scripts": {
    "start": "node server.js",
    "test": "node --test src/banking.test.js src/postgres-repository.test.js src/postgres-e2e.test.js ../tests/server.test.js",
    "check": "node --check server.js && node --check src/banking.js && node --check src/postgres-repository.js && node --check src/postgres-e2e.test.js",
    "db:health": "node -e \"require('./src/postgres-repository').createPostgresBank().health().then(x=>{console.log(x);return process.exit(0)}).catch(e=>{console.error(e.message);process.exit(1)})\""
  },
  "dependencies": {
    "pg": "^8.22.0"
  },
  "engines": { "node": ">=20" }
}
